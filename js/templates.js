//  back thik and caret keyword keyboard button uner esc buton 

const multiLine = `
   first line code
   second line code 
   third line code 
   four line code
   five line code 
   six line code ;
   

// `
// console.log(multiLine);

// const a = 20;
// const b = 30;

// const summary = 'sum of:' + a +' 'and' + b + ' 'is:'+(a+b);
// console.log(summary)

const a = 20;
const b = 30;

const summary = 'sum of: ' + a + ' and ' + b + ' is: ' + (a + b);
console.log(summary);


// const newSummary = `sum of ${a} and ${b} is : ${a + b}`;

// console.log(newSummary);

const newSummary = `sum of ${a} and ${b} is ${a + b}`;
console.log(newSummary);